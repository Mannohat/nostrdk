---
import NostrLayout from "../layouts/NostrLayout.astro";
---

<NostrLayout
	title="Nostr.dk – Tag del i fremtidens decentraliserede internet"
	description="Din indgang til Nostr – den censurresistente, decentraliserede protokol for sociale medier og meget mere."
>
	<!-- Hero Section -->
	<section class="hero">
		<h1>Tag del i fremtidens decentraliserede internet</h1>
		<p>
			Nostr giver dig ejerskab over dine data – en åben protokol for
			sociale medier og meget mere.
		</p>
		<button class="cta" id="start-button" data-umami-event="hero-button"
			>Lad mig læse mere om det</button
		>
	</section>

	<!-- Collapsible Content Sections -->
	<div
		class="collapsible-section"
		role="button"
		tabindex="0"
		aria-expanded="false"
		onclick="toggleSection(this)"
		onkeydown="if(event.key==='Enter'||event.key===' ')toggleSection(this)"
	>
		<div class="collapsible-header">1. Hvad er Nostr?</div>
		<div class="collapsible-content">
			<p>
				Nostr er en decentral protokol, der bl.a. giver brugerne
				mulighed for at kommunikere og dele indhold uden at være
				afhængige af central kontrol eller servere. Dette betyder, at
				der ikke er nogen enkelt enhed, der ejer eller styrer netværket,
				hvilket gør Nostr censurresistent og mere robust end
				traditionelle sociale platforme.
			</p>
			<p>
				<strong
					>Nostr står for "Notes and Other Stuff Transmitted by
					Relays".</strong
				>
			</p>
			<p>
				Nostr bruger offentlige og private nøgler til at identificere
				brugere. Hver bruger har et nøglepar og man kan have flere hvis
				man ønsker det:
			</p>
			<ul>
				<li>
					<strong>Offentlig nøgle (npub)</strong>: Denne fungerer som
					din identitet på Nostr og kan sammenlignes med et
					brugernavn. Din offentlige nøgle, også kaldet "npub", er
					synlig for alle og bruges til at identificere dig, når du
					sender beskeder eller opdaterer din profil.
				</li>
				<li>
					<strong>Privat nøgle (nsec)</strong>: Dette er din
					adgangskode, som er yderst vigtig at beskytte. Din private
					nøgle, også kaldet "nsec", bruges til at signere alle dine
					beskeder og handlinger på Nostr. Hvis nogen får adgang til
					din private nøgle, kan de overtage din konto, og der er
					ingen måde at nulstille eller gendanne den på. Derfor er det
					vigtigt at opbevare din private nøgle sikkert, fx i en
					password manager.
				</li>
			</ul>
		</div>
	</div>

	<div
		class="collapsible-section"
		role="button"
		tabindex="0"
		aria-expanded="false"
		onclick="toggleSection(this)"
		onkeydown="if(event.key==='Enter'||event.key===' ')toggleSection(this)"
	>
		<div class="collapsible-header">2. Hvorfor er Nostr vigtigt?</div>
		<div class="collapsible-content">
			<p>
				Som nævnt er Nostr en protokol og man kan allerede en masse ting
				med protokollen som vi ikke afdækker i denne introduktion.
				MEEEEN! en væsentlig -og lige nu klart den mest udbredte brug af
				Nostr er inden for sociale netværk, da det løser nogle af de
				store udfordringer som eksisterende sociale medier står overfor:
			</p>
			<ul>
				<li>
					<strong>Censur</strong>: Traditionelle sociale
					medieplatforme er underlagt kontrol fra centraliserede
					myndigheder, som kan censurere eller slette indhold. På
					Nostr vælger brugerne selv, hvilke relæer de vil forbinde
					til, og ingen enkelt enhed kan styre, hvad der bliver
					postet.
				</li>
				<li>
					<strong>Datasikkerhed</strong>: På centrale platforme som
					Facebook og Twitter opbevares alle dine data på deres
					servere, og du har ingen kontrol over, hvordan de bruges
					eller opbevares. På Nostr kan brugerne selv vælge, hvilke
					relæer deres data sendes til, og ingen tredjeparter har
					adgang til dine private nøgler eller beskeder.
				</li>
				<li>
					<strong>Privatliv</strong>: Nostr giver dig fuld kontrol
					over dine handlinger og beskeder. Hver besked, du sender, og
					hver opdatering, du foretager på din profil, signeres med
					din private nøgle. Dette sikrer, at det kun er dig med din
					private nøgle (nsec) som kan udføre disse handlinger. Med en
					Nostr-adresse, som du kan læse mere om længere nede på siden
					her, har du mulighed for at sikrer dig og dem der vil følge
					dig at de finder den korrekte profil og altså ikke en kopi
					som måtte forsøge at udgive sig for at være dig.
				</li>
			</ul>
		</div>
	</div>

	<div
		class="collapsible-section"
		role="button"
		tabindex="0"
		aria-expanded="false"
		onclick="toggleSection(this)"
		onkeydown="if(event.key==='Enter'||event.key===' ')toggleSection(this)"
	>
		<div class="collapsible-header">3. Hvordan fungerer Nostr?</div>
		<div class="collapsible-content">
			<p>
				Nostr fungerer ved hjælp af et netværk af relæer, der
				videresender beskeder mellem brugere. Disse relæer opbevarer
				ikke dine data permanent, men distribuerer dem til andre brugere
				og applikationer, du er forbundet med. Alle som har de tekniske
				forudsætninger for at opsætte et nostr relæ kan gøre det men det
				er ikke nødvendigt for at du kan bruge nostr og langt de fleste
				klienter er allerede opsat med en række af relæer fra start. Man
				kan til enhver tid fjerne eller tilføje relæer.
			</p>
			<p><strong>Oprettelse af en Nostr-konto:</strong></p>
			<ul>
				<li>
					<strong>Vælg en klient</strong>: For at bruge Nostr skal du
					vælge en klient. Klienter er de applikationer, der forbinder
					dig til Nostr-netværket. Der findes klienter til både web,
					desktop og mobil, så du kan vælge den, der passer bedst til
					dine behov.
				</li>
				<li>
					<strong>Generer dine nøgler</strong>: Når du opretter en
					konto i en Nostr-klient, genereres der automatisk et
					offentligt og privat nøglepar. Din offentlige nøgle (npub)
					fungerer som dit "brugernavn", og din private nøgle (nsec)
					bruges til at signere dine handlinger.
				</li>
				<li>
					<strong>Gem din private nøgle sikkert</strong>: Da din
					private nøgle fungerer som en adgangskode, er det vigtigt at
					opbevare den sikkert. Brug evt. en password manager til at
					beskytte din nøgle, så du ikke mister den.
				</li>
			</ul>
		</div>
	</div>

	<div
		class="collapsible-section"
		role="button"
		tabindex="0"
		aria-expanded="false"
		onclick="toggleSection(this)"
		onkeydown="if(event.key==='Enter'||event.key===' ')toggleSection(this)"
	>
		<div class="collapsible-header">4. Kom i gang med Nostr</div>
		<div class="collapsible-content">
			<p>
				At komme i gang med Nostr er enkelt, men der er nogle vigtige
				skridt, du skal følge for at beskytte din konto og få den bedst
				mulige oplevelse:
			</p>
			<ul>
				<li>
					<strong>Vælg en klient</strong>: Vælg en Nostr-kompatibel
					klient, såsom:
					<ul>
						<li>Damus (iOS)</li>
						<li>Primal (Android, iOS & Desktop)</li>
					</ul>
				</li>
				<li>
					<strong>Tilføj kontakter</strong>: Når du er sat op, kan du
					begynde at opbygge dit netværk ved at tilføje andre brugere
					som du vil følge. Du kan finde brugere ved at søge efter
					deres offentlige nøgle eller ved at tilføje deres
					NIP-05-identitet. For at finde andre danskere på Nostr kan
					du kigge på siden her <a
						href="https://find.nostr.dk"
						target="_blank"
						rel="noopener noreferrer">find.nostr.dk</a
					> ligesom du også kan følge hashtags i flere af de kendte klienter
					som f.eks. Damus og Primal.
				</li>
				<li>
					<strong>Verifikation via NIP-05</strong>: Du kan opsætte en
					Nostr-adresse, der knytter en genkendelig identitet, som
					f.eks. 'alias@nostr.dk', til din offentlige nøgle. Dette gør
					det lettere for andre at finde dig og bekræfte ægtheden af
					din profil, da det forhindrer, at nogen laver en kopi for at
					udgive sig for dig.
				</li>
				<li>
					<strong>Opsæt din Lightning-wallet (valgfrit)</strong>:
					Nostr understøtter betalinger via Lightning-netværket,
					hvilket gør det muligt at sende og modtage små donationer,
					kaldet 'zaps'. Dette er ikke nødvendigt for at bruge Nostr,
					men det er en spændende og sjov måde at betale folk på, hvis
					du f.eks. synes om et opslag, en artikel eller noget andet.
					Det giver også mulighed for, at andre kan betale dig. Denne
					model er kendt som Value4Value og bliver i stigende grad
					benyttet. For at bruge denne funktion skal du opsætte en
					Lightning-wallet, som kan forbindes til din Nostr-konto.
					Hvis du ikke er bekendt med Value4Value, Bitcoin eller
					Lightning-netværket, kan du besøge <a
						href="https://www.enogtyve.org"
						target="_blank"
						rel="noopener noreferrer">Enogtyve</a
					> for at lære mere.
				</li>
			</ul>
		</div>
	</div>

	<!-- Nostr Klienter Section -->
	<section class="nostr-klienter" id="nostr-klienter">
		<h2>Nostr Klienter</h2>
		<p>
			Der findes mange Nostr-klienter og tjenester, men her er nogle
			forslag til at komme i gang. Primal og Damus minder om Twitter og
			viser informationer på en måde, du måske kender fra Twitter. Primal
			kan bruges på Android, iOS og desktop, mens Damus kun er tilgængelig
			på iOS. Hvis du vil lære mere om Bitcoin og Lightning eller prøve
			Value4Value-modellen, er Primal nok den nemmeste at starte med. Husk
			at man altid kan skifte til en anden klient og alle dine data vil
			følge med.
		</p>
		<div class="klient-container">
			<div class="klient-item">
				<a href="https://primal.net" target="_blank" data-umami-event="klient-primal">
					<img src="/primal.png" alt="Primal" loading="lazy" />
					<h3>Primal</h3>
				</a>
				<p class="klient-description">Android, iOS & Desktop</p>
				<a href="https://primal.net" target="_blank" class="klient-link" data-umami-event="klient-primal">Besøg Primal</a>
			</div>
			<div class="klient-item">
				<a href="https://damus.io" target="_blank" data-umami-event="klient-damus">
					<img src="/damus.png" alt="Damus" loading="lazy" />
					<h3>Damus</h3>
				</a>
				<p class="klient-description">iOS</p>
				<a href="https://damus.io" target="_blank" class="klient-link" data-umami-event="klient-damus">Besøg Damus</a>
			</div>
		</div>
	</section>

	<!-- NIP-05 Section -->
	<section class="nostr-nip05" id="nostr-nip05">
		<h2>
			Få en Nostr adresse <span style="font-size: 0.75em;">(NIP-05 adresse)</span>
		</h2>
		<p>
			Du kan opsætte en verificeret identitet som gør det muligt at knytte
			en genkendelig adresse (f.eks. "alias@nostr.dk") til din offentlige
			nøgle. Dette gør det nemmere for andre at finde dig og bekræfte, at
			din profil er din og ikke nogen der udgiver sig for at være dig.
			Tjek om dit ønskede navn er ledigt herunder, følg anvisningerne og
			betal 2.100 sats via Lightning — din adresse er aktiv inden for få
			minutter.
		</p>

		<!-- Profil teaser -->
		<div class="nip05-teaser">
			<p class="nip05-teaser-label">Allerede verificeret med @nostr.dk</p>
			<div class="nip05-teaser-grid" id="nip05TeaserGrid"></div>
			<p class="teaser-see-more"><a href="/profiler">Se flere profiler →</a></p>
		</div>

		<!-- Inline registreringsformular -->
		<div class="nip05-register">
			<!-- Trin 1: Navn-tjek -->
			<div id="nip05-step1">
				<div class="nip05-field">
					<div class="nip05-name-row" id="nip05NameRow">
						<input
							type="text"
							id="nip05Name"
							placeholder="ditnavnher"
							autocomplete="off"
							autocapitalize="none"
							spellcheck="false"
						/>
						<span class="nip05-suffix">@nostr.dk</span>
					</div>
					<div class="nip05-status" id="nip05Status"></div>
				</div>
				<p class="nip05-price">
					@nostr.dk nip-05 adresse for kun <strong>2.100 sats</strong> via
					<a href="https://enogtyve.org/da/laer-mere/lightning/" target="_blank" rel="noopener noreferrer">Lightning</a> ⚡
				</p>
				<button id="nip05ProceedBtn" class="cta" disabled data-umami-event="nip05-button">Næste</button>
			</div>

			<!-- Trin 2: Indtast npub og betal -->
			<div id="nip05-step2" style="display: none;">
				<div class="nip05-confirmed" id="nip05Confirmed"></div>
				<div class="nip05-field" style="margin-bottom: 1rem;">
					<label style="display:block; font-size:0.85rem; font-weight:600; color:#e0e0e0; margin-bottom:0.5rem; text-transform:uppercase; letter-spacing:0.05em;">Din Nostr public key</label>
					<input
						type="text"
						id="nip05Pubkey"
						placeholder="npub1… eller hex"
						autocomplete="off"
						autocapitalize="none"
						spellcheck="false"
						style="width:100%; background:rgba(0,0,0,0.3); border:1px solid rgba(255,255,255,0.1); border-radius:6px; padding:0.7rem 0.9rem; color:#fff; font-size:0.9rem; outline:none; box-sizing:border-box; transition: border-color 0.2s;"
					/>
					<div class="nip05-status" id="nip05PubkeyStatus"></div>
				</div>
				<div class="nip05-instructions">
					<p>Hvad sker der næste:</p>
					<ol>
						<li>Klik "Gå til betaling ⚡"</li>
						<li>Betal 2.100 sats via Lightning</li>
						<li>Din NIP-05 adresse aktiveres automatisk</li>
					</ol>
				</div>
				<button id="nip05PayBtn" class="cta" disabled data-umami-event="nip05-pay-button">Gå til betaling ⚡</button>
				<div class="nip05-status" id="nip05PayStatus"></div>
				<button id="nip05BackBtn" class="nip05-back-btn">← Vælg et andet navn</button>
			</div>
		</div>
	</section>

	<!-- Nostr Ressourcer Section -->
	<section class="nostr-ressourcer" id="nostr-ressourcer">
		<h2>Nostr Ressourcer</h2>
		<p>
			De fleste tjenester og virksomheder som YouTube, Google, Facebook,
			Twitter, Instagram, Meetups.com, Pinterest osv. har adgang til dine
			data og kan lukke din konto, hvis de ønsker det. Med Nostr får du
			mulighed for at eje dine egne data, hvilket sikrer, at du altid har
			adgang til din konto. Du kan også bevare dine opslag, følgere og
			dem, du følger, på tværs af alle Nostr-baserede tjenester. Det
			betyder også at du nemt kan skifte til en anden platform/klient.
		</p>
		<div class="resource-container">
			<a href="/podcasts" class="resource-item" data-umami-event="ressource-podcasts">
				<img src="/podcasts.png" alt="Podcasts" class="resource-icon" loading="lazy" />
				<h3>Nostr Podcasts</h3>
			</a>
			<a href="/apps" class="resource-item" data-umami-event="ressource-apps">
				<img src="/apps.png" alt="Apps & Tjenester" class="resource-icon" loading="lazy" />
				<h3>Apps & Tjenester</h3>
			</a>
			<a href="https://t.me/enogtyvedk/510" target="_blank" class="resource-item" data-umami-event="telegram-link">
				<img src="/telegram.png" alt="Telegram" class="resource-icon" loading="lazy" />
				<h3>Nostr.dk Telegram</h3>
			</a>
		</div>
	</section>

	<!-- Om Section -->
	<section class="nostr-om" id="nostr-om">
		<h2>Om Nostr.dk</h2>
		<p>
			Formålet med Nostr.dk er at være en dansk ressource, der hjælper
			nysgerrige med at opnå en overordnet forståelse af protokollen samt
			nemt komme i gang med at bruge fremtidens protokol for social
			interaktion. Der findes masser af gode ressourcer på engelsk, som du
			nemt kan finde ved at søge på Nostr.
		</p>
		<p>
			Har du spørgsmål så hop ind i telegram gruppen som du finder link
			til herover. Ønsker du at komme i kontakt med Nostr.dk så send os
			gerne en Email.
		</p>
		<div class="resource-container">
			<a href="mailto:nostr@nostr.dk" data-umami-event="email-link">
				<img src="/sendmail.png" alt="Send Email" class="resource-icon" loading="lazy" />
				<h3>Send Email</h3>
			</a>
		</div>
	</section>

	<!-- Support Section -->
	<section class="nostr-support" id="nostr-support">
		<h2>Støt op om Nostr.dk</h2>
		<p>
			Hvis du vil støtte vores arbejde med at udbrede Nostr i Danmark, kan
			du sende lidt sats ⚡️ via Lightning-netværket. Hvad er Lightning? <a
				href="https://enogtyve.org/da/laer-mere/lightning/"
				target="_blank"
				rel="noopener noreferrer">Lær mere her</a>.
		</p>
		<button
			onclick="copyToClipboard()"
			style="background: none; border: none; padding: 10px; cursor: pointer;"
			type="button"
			aria-label="Klik for at kopiere LNURL til udklipsholder"
			data-umami-event="support-qr-click"
		>
			<img src="/support-qr.png" alt="Support QR Code" class="support-qr" loading="lazy" />
		</button>
		<p class="support-instruction"><em>Klik på QR-koden for at kopiere LNURL til udklipsholder</em></p>
	</section>

	<script is:inline>
		function toggleSection(element) {
			element.classList.toggle("expanded");
			element.setAttribute("aria-expanded", element.classList.contains("expanded") ? "true" : "false");
		}
		window.toggleSection = toggleSection;

		window.copyToClipboard = async function () {
			const lnurl = "LNURL1DP68GURN8GHJ7ER9D4HJUMRWWP5KWEME9E3K7MF0D3H82UNVWQH4JAR4DYM5XKGH6R5";
			if (navigator.clipboard && navigator.clipboard.writeText) {
				try { await navigator.clipboard.writeText(lnurl); alert("LNURL kopieret til udklipsholder!"); return; } catch (err) {}
			}
			const textArea = document.createElement("textarea");
			textArea.value = lnurl;
			textArea.style.position = "fixed";
			textArea.style.left = "-999999px";
			document.body.appendChild(textArea);
			textArea.focus();
			textArea.select();
			try { document.execCommand("copy"); alert("LNURL kopieret til udklipsholder!"); } catch (err) { alert("Kunne ikke kopiere. Prøv venligst igen."); }
			document.body.removeChild(textArea);
		};

		function validatePubkey(val) {
			return /^npub1[a-z0-9]{58}$/.test(val) || /^[0-9a-f]{64}$/.test(val);
		}

		document.addEventListener("DOMContentLoaded", function () {
			// Hero knap
			const startButton = document.getElementById("start-button");
			if (startButton) {
				startButton.addEventListener("click", function () {
					const sections = document.querySelectorAll(".collapsible-section");
					sections.forEach((s) => { s.classList.add("expanded"); s.setAttribute("aria-expanded", "true"); });
					setTimeout(() => {
						const first = sections[0];
						if (first) window.scrollTo({ top: first.getBoundingClientRect().top + window.scrollY - 80, behavior: "smooth" });
					}, 300);
				});
			}

			// NIP-05 profil teaser
			(async function loadTeaser() {
				try {
					const res = await fetch("/.well-known/nostr.json");
					const data = await res.json();
					const names = data.names || {};
					const entries = Object.entries(names).sort(() => Math.random() - 0.5).slice(0, 20);
					const pubkeys = entries.map(([, pk]) => pk);
					const teaserGrid = document.getElementById("nip05TeaserGrid");
					if (!teaserGrid) return;

					teaserGrid.style.cssText = "display:flex; flex-wrap:wrap; justify-content:center; gap:8px 10px; align-items:flex-start; padding-bottom:40px;";

					const profileRes = await fetch(`/.netlify/functions/nostr-profiles?pubkeys=${pubkeys.join(",")}`);
					const profiles = await profileRes.json();
					const withPicture = entries.filter(([, pk]) => profiles[pk]?.picture).slice(0, 8);

					const isMobile = window.innerWidth < 768;
					const colWidth = isMobile ? "56px" : "72px";
					const imgSize = isMobile ? "48px" : "64px";
					const staggerOffset = isMobile ? "0px" : "36px";

					let colIndex = 0, col = null, cardsInCol = 0;
					for (const [, pk] of withPicture) {
						const profile = profiles[pk];
						if (!col || cardsInCol >= 2) {
							col = document.createElement("div");
							col.style.cssText = `display:flex; flex-direction:column; align-items:center; gap:10px; flex:0 0 ${colWidth};`;
							if (colIndex % 2 === 1) col.style.marginTop = staggerOffset;
							teaserGrid.appendChild(col);
							colIndex++; cardsInCol = 0;
						}
						const img = document.createElement("img");
						img.style.cssText = `width:${imgSize}; height:${imgSize}; border-radius:50%; object-fit:cover; border:2px solid rgba(108,79,150,0.6); display:block;`;
						img.src = profile.picture; img.alt = ""; img.loading = "lazy"; img.onerror = () => {};
						col.appendChild(img); cardsInCol++;
					}
				} catch {}
			})();

			// NIP-05 navn-tjek
			let nip05NameAvailable = false, nip05CheckTimeout, confirmedName = "";

			function nip05SetStatus(type, icon, text) {
				document.getElementById("nip05Status").className = "nip05-status " + type;
				document.getElementById("nip05Status").textContent = icon + " " + text;
				document.getElementById("nip05NameRow").className = "nip05-name-row " + (type === "available" ? "valid" : type === "taken" ? "taken" : "");
			}

			function nip05ClearStatus() {
				document.getElementById("nip05Status").className = "nip05-status";
				document.getElementById("nip05Status").textContent = "";
				document.getElementById("nip05NameRow").className = "nip05-name-row";
				nip05NameAvailable = false;
				document.getElementById("nip05ProceedBtn").disabled = true;
			}

			async function nip05CheckAvailability(name) {
				if (!name) { nip05ClearStatus(); return; }
				if (!/^[a-z0-9_-]+$/.test(name)) {
					nip05SetStatus("taken", "✗", "Kun små bogstaver, tal, _ og - er tilladt");
					nip05NameAvailable = false;
					document.getElementById("nip05ProceedBtn").disabled = true;
					return;
				}
				nip05SetStatus("checking", "…", "Tjekker tilgængelighed…");
				try {
					const res = await fetch("/.netlify/functions/check-name?name=" + encodeURIComponent(name));
					const data = await res.json();
					if (data.available) {
						nip05SetStatus("available", "✓", "Ledigt!");
						nip05NameAvailable = true;
						document.getElementById("nip05ProceedBtn").disabled = false;
					} else {
						nip05SetStatus("taken", "✗", "Navnet er allerede taget — prøv et andet");
						nip05NameAvailable = false;
						document.getElementById("nip05ProceedBtn").disabled = true;
					}
				} catch {
					nip05SetStatus("error", "!", "Kunne ikke tjekke — prøv igen");
					nip05NameAvailable = false;
					document.getElementById("nip05ProceedBtn").disabled = true;
				}
			}

			const nameInput = document.getElementById("nip05Name");
			if (nameInput) {
				nameInput.addEventListener("input", function () {
					const val = this.value.toLowerCase().replace(/[^a-z0-9_-]/g, "");
					if (this.value !== val) this.value = val;
					clearTimeout(nip05CheckTimeout);
					if (!val) { nip05ClearStatus(); return; }
					nip05CheckTimeout = setTimeout(() => nip05CheckAvailability(val), 500);
				});
			}

			const proceedBtn = document.getElementById("nip05ProceedBtn");
			if (proceedBtn) {
				proceedBtn.addEventListener("click", function () {
					if (!nip05NameAvailable) return;
					confirmedName = document.getElementById("nip05Name").value.trim();
					document.getElementById("nip05Confirmed").textContent = "✓ " + confirmedName + "@nostr.dk er ledigt";
					document.getElementById("nip05-step1").style.display = "none";
					document.getElementById("nip05-step2").style.display = "block";
				});
			}

			// Pubkey validering — live feedback
			const pubkeyInput = document.getElementById("nip05Pubkey");
			const pubkeyStatus = document.getElementById("nip05PubkeyStatus");
			const payBtn = document.getElementById("nip05PayBtn");

			if (pubkeyInput) {
				pubkeyInput.addEventListener("input", function () {
					const val = this.value.trim();
					if (!val) {
						pubkeyStatus.className = "nip05-status";
						pubkeyStatus.textContent = "";
						pubkeyInput.style.borderColor = "rgba(255,255,255,0.1)";
						payBtn.disabled = true;
						return;
					}
					if (validatePubkey(val)) {
						pubkeyStatus.className = "nip05-status available";
						pubkeyStatus.textContent = "✓ Gyldig public key";
						pubkeyInput.style.borderColor = "#4caf50";
						payBtn.disabled = false;
					} else {
						pubkeyStatus.className = "nip05-status taken";
						pubkeyStatus.textContent = "✗ Skal være npub1… (63 tegn) eller hex (64 tegn)";
						pubkeyInput.style.borderColor = "#e05050";
						payBtn.disabled = true;
					}
				});
			}

			if (payBtn) {
				payBtn.addEventListener("click", async function () {
					const pubkey = pubkeyInput.value.trim();
					if (!validatePubkey(pubkey)) return;

					payBtn.disabled = true;
					payBtn.textContent = "Opretter betaling…";
					const ps = document.getElementById("nip05PayStatus");
					ps.className = "nip05-status checking";
					ps.textContent = "… Forbinder til BTCPay…";

					try {
						const res = await fetch("/.netlify/functions/create-nip05-invoice", {
							method: "POST",
							headers: { "Content-Type": "application/json" },
							body: JSON.stringify({ name: confirmedName, pubkey }),
						});
						const data = await res.json();
						if (data.checkoutLink) {
							window.location.href = data.checkoutLink;
						} else {
							throw new Error("Intet checkout link");
						}
					} catch (err) {
						ps.className = "nip05-status error";
						ps.textContent = "! Noget gik galt — prøv igen";
						payBtn.disabled = false;
						payBtn.textContent = "Gå til betaling ⚡";
					}
				});
			}

			const backBtn = document.getElementById("nip05BackBtn");
			if (backBtn) {
				backBtn.addEventListener("click", function () {
					document.getElementById("nip05-step2").style.display = "none";
					document.getElementById("nip05-step1").style.display = "block";
				});
			}
		});
	</script>

	<style>
		.klient-description {
			font-size: 0.85rem;
			color: rgba(255, 255, 255, 0.5);
			margin: 8px 0;
			font-weight: 400;
			text-align: center;
		}

		.support-instruction {
			text-align: center;
			margin-top: 15px;
			font-size: 1rem;
		}

		.support-qr {
			cursor: pointer;
			touch-action: manipulation;
			-webkit-user-select: none;
			user-select: none;
			width: 90%;
			max-width: 200px;
			display: block;
			margin: 20px auto;
			padding: 10px;
			transition: transform 0.2s ease;
		}

		@media (max-width: 768px) {
			.support-qr { max-width: 320px; }
		}

		.support-qr:active { transform: scale(0.95); }

		.nip05-teaser { margin: 10px 0 30px; }

		.nip05-teaser-label {
			font-size: 0.8rem;
			color: rgba(255, 255, 255, 0.4);
			text-align: center;
			margin-bottom: 14px;
			letter-spacing: 0.04em;
			text-transform: uppercase;
		}

		.nip05-teaser-grid {
			display: flex;
			flex-wrap: wrap;
			justify-content: center;
			gap: 8px 10px;
			align-items: flex-start;
		}

		.nip05-register {
			max-width: 480px;
			margin: 30px auto 10px;
			text-align: left;
		}

		.nip05-field { margin-bottom: 1rem; }

		.nip05-name-row {
			display: flex;
			align-items: stretch;
			background: rgba(0, 0, 0, 0.3);
			border: 1px solid rgba(255, 255, 255, 0.1);
			border-radius: 6px;
			overflow: hidden;
			transition: border-color 0.2s;
		}

		.nip05-name-row:focus-within { border-color: rgba(108, 79, 150, 0.8); }
		.nip05-name-row.valid { border-color: #4caf50; }
		.nip05-name-row.taken { border-color: #e05050; }

		.nip05-name-row input {
			flex: 1;
			background: transparent;
			border: none;
			outline: none;
			padding: 0.7rem 0.9rem;
			font-family: "Inter", sans-serif;
			font-size: 1rem;
			color: #fff;
		}

		.nip05-name-row input::placeholder { color: rgba(255, 255, 255, 0.3); }

		.nip05-suffix {
			padding: 0 0.75rem;
			color: rgba(255, 255, 255, 0.4);
			font-size: 0.9rem;
			white-space: nowrap;
			border-left: 1px solid rgba(255, 255, 255, 0.1);
			background: rgba(0, 0, 0, 0.2);
			display: flex;
			align-items: center;
		}

		.nip05-status {
			font-size: 0.82rem;
			margin-top: 0.4rem;
			min-height: 1.1em;
		}

		.nip05-status.checking { color: rgba(255, 255, 255, 0.4); }
		.nip05-status.available { color: #4caf50; }
		.nip05-status.taken { color: #e05050; }
		.nip05-status.error { color: #e08050; }

		.nip05-price {
			font-size: 0.9rem;
			color: rgba(255, 255, 255, 0.6);
			margin: 0.6rem 0 0;
		}

		.nip05-price strong { color: #fff; }

		.nip05-register .cta {
			margin-top: 1rem;
			width: 100%;
			text-align: center;
			display: block;
		}

		.nip05-register .cta:disabled {
			opacity: 0.3;
			cursor: not-allowed;
			transform: none;
		}

		.nip05-confirmed {
			font-size: 1.2rem;
			font-weight: 700;
			color: #4caf50;
			margin-bottom: 1rem;
		}

		.nip05-instructions {
			background: rgba(0, 0, 0, 0.2);
			border: 1px solid rgba(255, 255, 255, 0.08);
			border-radius: 8px;
			padding: 0.9rem 1.1rem;
			margin-bottom: 0.5rem;
		}

		.nip05-instructions p {
			font-size: 0.9rem;
			color: #e0e0e0;
			margin-bottom: 0.6rem;
			text-align: left;
		}

		.nip05-instructions ol {
			padding-left: 1.2rem;
			color: #d0d0d0;
			font-size: 0.88rem;
			line-height: 1.8;
			text-align: left;
			margin-bottom: 0;
		}

		.nip05-instructions strong { color: #fff; }
		.nip05-instructions em { color: rgba(255, 255, 255, 0.55); font-style: normal; }

		.nip05-back-btn {
			background: none;
			border: none;
			color: rgba(255, 255, 255, 0.4);
			font-size: 0.82rem;
			cursor: pointer;
			margin-top: 0.8rem;
			padding: 0;
			display: block;
			transition: color 0.2s;
		}

		.nip05-back-btn:hover { color: rgba(255, 255, 255, 0.7); }
	</style>
</NostrLayout>